Для усановки и запуска проекта необходимо:
1.Установить в соответствии с документацией Community версию QT5 и QTCreator с сайта qt-project.org.
2.Скачать и скомпиллировать проект https://github.com/Justice01/bnb-simulator в соответствии с приложенной инструкцией.
3.Перейти в директорию с проектом bnb-batch и выполнить QT_SELECT=qt5 qmake.
4.Выполнить make.
5.В папку с проектом добавить из проекта simulator-bnb файлы bnbtest и settings.json.
6.Запустить bnb-batch, передав ему 2 параметра:
	-файл со списком параметров, где:
	a)первая строка используется для описания имен параметров. После обработки она копируется в файл с результатами, и к ней добавляются Speedup и Efficiency; 
	б)каждая строка, начиная со второй, представляет собой набор параметров, разделенных пробелом. Набор параметров состоит из:
		-параметров json в следующем порядке "use the same trees", "seed", "latency", "bandwidth", "maximal task level";
		-количества процессоров;
		-количество первоначальных ветвлений для балансировщика;
		-иные параметры балансировщика.
	-файл с результатами. Сюда будут записываться результаты работы программы в следующем формате:
	а)первая строка копируется из файла со списком параметров, и к ней добавляются Speedup и Efficiency;
	б)каждая строка, начиная со второй, повторяет соответствующий набор параметров из файла со списком параметров, плюс к ней добавляются ускорение и эффективность.

ВНИМАНИЕ: для запуска программы нужны права для создания, изменения и удаления файлов!
